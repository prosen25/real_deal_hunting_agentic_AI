# price_prediction_AI_app
This app uses AI which is trained specifically to predict the price of a product based on the description.

Setup:
Step 1: Deploy the trained model in modal using command 'uv run modal deploy -m pricer_service'